mutation SocialAuth($code: String!, $provider: String!, $appId: String) {
  socialAuth(provider: $provider, code: $code, appId: $appId) {
    token
    tokenExpiresIn
    refreshToken
    refreshExpiresIn
  }
}
