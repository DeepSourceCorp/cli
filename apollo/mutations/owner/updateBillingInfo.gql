mutation($ownerId: ID!, $billingEmail: String, $billingAddress: String) {
  updateBillingInfo(
    input: { id: $ownerId, billingEmail: $billingEmail, billingAddress: $billingAddress }
  ) {
    billingEmail
    billingAddress
  }
}
