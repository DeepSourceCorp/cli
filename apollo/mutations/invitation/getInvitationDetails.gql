mutation($invitationCode: String!) {
  confirmInvitation(input: { action: VIEW, invitationCode: $invitationCode }) {
    teamName
    teamLogo
    vcsProvider
    role
    joined
    viewer {
      firstName
      lastName
      avatar
    }
  }
}
