mutation($repoIssueId: ID, $checkId: ID, $filePath: String, $issueShortcode: String) {
  ignoreIssueForRepository(
    input: {
      repoIssueId: $repoIssueId
      checkId: $checkId
      issueShortcode: $issueShortcode
      filePath: $filePath
    }
  ) {
    ok
    checkIssueIds
  }
}
