query($shortcode: String!, $objectId: ID!, $level: IssuePriorityLevel!) {
  issue(shortcode: $shortcode) {
    id
    issuePriority(level: $level, objectId: $objectId) {
      cascadingIssuePriority {
        slug
        verboseName
        weight
      }
      source
    }
  }
}
