query issueOccurrenceDistributionByProduct(
  $provider: VCSProviderChoices!
  $owner: String!
  $name: String!
) {
  repository(provider: $provider, name: $name, owner: $owner) {
    id
    issueOccurrenceDistributionByProduct {
      key
      count
    }
  }
}
