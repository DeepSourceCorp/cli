query publicReportAnalyzerDistribution($key: String!, $reportId: String!, $token: String) {
  publicReport(reportId: $reportId, token: $token) {
    id
    issueDistributionByAnalyzer(key: $key) {
      slug
      name
      value
      logoUrl
    }
  }
}
